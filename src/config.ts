/**
 * If LinkProxy2 should use HTTPS with its self signed certificate. this can be toggled off for i.e. NodeJS bots that mkcert seems to not install its CA authority into?
 */
export const ENABLE_HTTPS = true;
